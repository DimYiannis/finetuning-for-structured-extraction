[project]
name = "finetuning-structured-extract"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.9",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
]

[tool.uv]
package = false
