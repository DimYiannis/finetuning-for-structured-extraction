[project]
name = "finetuning-structured-extract"
version = "0.1.0"
requires-python = ">=3.10,<3.14"
dependencies = [
    "pydantic>=2.9",
    "torch>=2.13.0",
    "transformers>=5.16.1",
    "outlines>=1.3.3",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
]

[tool.uv]
package = false
